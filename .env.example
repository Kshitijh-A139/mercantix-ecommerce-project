# Copy this to .env and fill in real values before running docker compose up

# JWT signing secret — must be 64+ random characters, keep it secret
JWT_SECRET=replace-this-with-a-long-random-string-at-least-64-characters-long
